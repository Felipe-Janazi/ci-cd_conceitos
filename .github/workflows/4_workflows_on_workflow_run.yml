name: Github sactions - Workflow run Triggado
run-name: Running - Workflow run Triggado
on: 
  workflow_run:
    # Assim que os workflows forem completados eles triggam este
    workflows: [Github Actions - Trigger 1, Github Actions - Trigger 1]
    types: [completed]
    branches: [main]

jobs: 
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Comentario
        run: |
          echo "Workflow que foi Triggado!"